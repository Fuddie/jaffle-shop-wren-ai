relationships:
- name: orders_customers
  models:
  - orders
  - customers
  join_type: MANY_TO_ONE
  condition: orders.customer_id = customers.customer_id
- name: payments_orders
  models:
  - payments
  - orders
  join_type: MANY_TO_ONE
  condition: payments.order_id = orders.order_id
